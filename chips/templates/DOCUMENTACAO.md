# Documentação — `chips/templates/`

Templates HTML do app **chips**.

## Para que serve

Renderiza telas do módulo de chips (dashboard, listas, formulários e atribuição).

## Subpastas

| Pasta | Conteúdo |
|-------|----------|
| `chips/` | Templates do namespace `chips/`. Ver `chips/DOCUMENTACAO.md`. |

## Arquivos neste nível

Nenhum `.html` na raiz de `templates/`; arquivos ficam em `templates/chips/`.
